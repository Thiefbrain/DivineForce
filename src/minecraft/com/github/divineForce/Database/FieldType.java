package com.github.divineForce.Database;

/**
 * The field types for a database column. Each table must have at least one key.
 * 
 * @author Thiefbrain
 */
public enum FieldType
{
    AUTO_KEY, AUTO_VALUE, KEY, VALUE
}
